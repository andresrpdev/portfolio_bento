---
const projects = [
{
    title:"ImportedJM", 
    description:"Trabajo para un cliente en el que importaba productos, con node js realice un scraper para obtener los datos de los productos y un api para poder usarlos en el frontend y calcular el precio final de importacion. Ademas de tener un modo manual",
    technologies:["React","Tailwind","Node.js","Express","MongoDB"],
    image:"https://cdn.palbincdn.com/images/blog/gallery/Partes-de-una-pagina-web.png",
    link:"google.com",
    github:"github.com"
},
{
    title:"",
    description:"",
    technologies:["React","Tailwind","Node.js","Express","MongoDB"], 
    image:"",
    link:"",
    github:""
}
]
---
<div class="grid grid-cols-2 gap-4">
    {projects.map(project => (
        <div class="relative overflow-hidden group aspect-square">
            <div class="absolute inset-0 bg-gradient-to-br from-violet-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-all duration-300"></div>
            <div class="relative flex flex-col gap-3 p-4 h-full rounded-xl bg-neutral-800/50 border border-neutral-700/50 hover:border-indigo-500/30 transition-all duration-300">
                <div class="flex justify-between items-start">
                    <h3 class="text-lg font-semibold text-gray-200 group-hover:text-white transition-colors">{project.title}</h3>
                </div>
                
                {project.image && (
                    <img 
                        src={project.image} 
                        alt={project.title} 
                        class="w-full h-40 object-cover rounded-lg"
                    />
                )}
                
                <p class="text-sm text-gray-400 group-hover:text-gray-300">{project.description}</p>
                <p class="text-xs text-gray-500">{project.technologies.join(", ")}</p>
                
                <div class="flex gap-4 mt-auto">
                    {project.link && (
                        <a 
                            href={project.link}
                            class="text-sm text-indigo-400 hover:text-indigo-300 transition-colors"
                            target="_blank"
                            rel="noopener noreferrer"
                        >
                            Ver proyecto →
                        </a>
                    )}
                    {project.github && (
                        <a 
                            href={project.github}
                            class="text-sm text-indigo-400 hover:text-indigo-300 transition-colors"
                            target="_blank"
                            rel="noopener noreferrer"
                        >
                            Ver código →
                        </a>
                    )}
                </div>
            </div>
        </div>
    ))}
</div>
